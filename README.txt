
README - رحلة عبر الزمن مع علماء الكيمياء (Streamlit app)

ملف التطبيق: app.py

ما الذي يفعله التطبيق؟
- تطبيق تفاعلي تعليمـي لعرض رحلات زمنية قصيرة مع علماء الكيمياء.
- كل محطة فيها مقدمة، محادثة بسيطة (مُبرمجة)، وتحدٍ قصير للطلاب.
- يمكن تشغيله محليًا في "وضع مُبرمج" دون حاجه للإنترنت أو مع ميزات "الوضع الذكي" في حال ربطه بـ OpenAI API (تحتاج لمفتاح API وتعديل بسيط).

التشغيل المحلي:
1. تأكد أن لديك Python 3.8+ مثبت.
2. ثبت streamlit:
   pip install streamlit
3. شغل التطبيق:
   streamlit run app.py
4. افتح المتصفح على الرابط المعروض (عادة http://localhost:8501)

إضافة وضع OpenAI (اختياري):
- نضف كود لاستدعاء OpenAI داخل جزء المحادثة في app.py باستخدام متغير البيئة OPENAI_API_KEY.
- تذكر أن هذا الكود غير مضمّن افتراضيًا. إن أردت أرسل لي لأدرجه لك.

تخصيص:
- يمكنك تعديل قائمة STATIONS داخل app.py لإضافة علماء أو تغيير الأسئلة.
- لتوليد صور لكل محطة، ادمج خدمات رسم AI (مطلوب اتصال إنترنت).

استمتع! 🎓🧪
